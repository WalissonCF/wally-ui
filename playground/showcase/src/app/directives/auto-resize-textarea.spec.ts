import { AutoResizeTextarea } from './auto-resize-textarea';

describe('AutoResizeTextarea', () => {
  it('should create an instance', () => {
    const directive = new AutoResizeTextarea();
    expect(directive).toBeTruthy();
  });
});
